{
  "name": "root",
  "private": true,
	"license" : "MIT",
	"author": "Norbert Herczeg <norbert.herczeg@blackbelt.hu>",
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test --stream",
    "format": "prettier --config .prettierrc 'packages/@pandino/*/src/**/*.ts' --write",
    "format-examples": "prettier --config .prettierrc examples/*/packages/*/src/**/*.ts --write"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
