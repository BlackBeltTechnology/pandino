{
  "name": "root",
  "private": true,
	"license" : "MIT",
	"author": "Norbert Herczeg <norbert.herczeg@blackbelt.hu>",
  "scripts": {
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run --scope @pandino/example start --stream",
    "test": "lerna run --scope @pandino/pandino test --stream",
    "format": "prettier --config .prettierrc packages/@pandino/*/src/**/*.ts --write"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
