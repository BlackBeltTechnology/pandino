{
  "name": "pokedex",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "format": "prettier --config .prettierrc \"packages/*/src/**/*.ts\" --write",
    "format-tsx": "prettier --config .prettierrc \"packages/*/src/**/*.tsx\" --write",
    "build": "lerna run build",
    "start": "lerna run --scope app start --stream"
  },
  "keywords": [
    "demo",
    "pandino"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
